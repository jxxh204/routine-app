## 📌 PR 요약
로컬 중심 MVP 완성도 보강(웹+모바일)을 진행했습니다.

- Today 루틴 폼 검증 강화(빈 제목/중복/시간 오류)
- 인증 이미지 스냅샷(localStorage) 보존 강화
- Calendar 날짜 상세 카드 정보 밀도 강화
- Today 카드 정렬 개선(지금 가능한 루틴 우선)
- Today 스와이프/스크롤 제스처 충돌 완화
- 모바일 WebView 탭 동기화 + 하단 탭바 가림 방지 + 네비 톤 정렬

## 🔎 재현 (Before)
- 잘못된 루틴 입력이 조용히 실패하거나 허용됨
- 캘린더 상세 정보가 부족해 확인 포인트가 약함
- today 스와이프가 스크롤과 충돌
- 모바일에서 탭바가 하단 내용을 가리거나 탭 상태가 경로와 어긋남

## 🧠 원인
- 폼 유효성/에러 노출 로직 부족
- 상세 패널 정보 구조 미흡
- 제스처 판정이 단순 X축 기준
- WebView route ↔ tab 동기화 및 하단 inset 처리 누락

## ✅ 해결 (After)
- 폼 에러 메시지/중복 체크/시간 검증 추가
- selected day 상세에 완료 수, 인증 이미지 수, 타입 칩, 빈 이미지 placeholder 추가
- actionable routine 우선 정렬
- swipe 인식 조건 강화(X/Y 비율 + 임계치)
- 모바일 탭 동기화(useEffect), body bottom inset, 탭/헤더 톤 정렬

## 🧪 QA
- `apps/web npm run build` 통과
- `apps/mobile npm run test` 통과

## 📷 스크린샷
- 기존 PR 코멘트 스크린샷 흐름 유지 (필요 시 추가 캡처 업로드)

## ⚠️ 리스크
- 로컬 중심 로직 보강으로 서버 연동 변경 없음
