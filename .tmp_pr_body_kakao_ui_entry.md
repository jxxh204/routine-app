## 📌 PR 요약
카카오 로그인 진입 UI를 단순화해, 첫 화면에서 바로 `카카오 로그인 -> 루틴앱 접속` 흐름이 보이도록 개선했습니다.

- auth 화면을 카카오 단일 진입 카드 UI로 정리
- 진입 카피를 로그인 후 즉시 앱 진입 중심으로 변경
- 기존 auth/session 복구 로직은 유지 (동작 변경 최소화)

## 🔎 문제 재현 (Before)
- [x] 재현 절차를 적었다
- [x] 기존 동작/에러를 캡처했다 (로그, 스크린샷, 영상 중 1개)

재현 절차:
1. 비로그인 상태로 `/auth?next=/today` 진입
2. 기존 auth 화면 확인

기존 결과:
- 소셜 로그인 진입 포인트가 분산되어 목적(카카오 로그인 후 앱 접속)이 약하게 보임

## 🧠 원인
- UI 우선순위가 인증 핵심 경로(카카오)보다 일반 정보성/다중 옵션 쪽에 분산됨

## ✅ 해결 (After)
변경 사항:
- `apps/web/src/app/auth/page.tsx`
  - 카카오 단일 진입 카드형 레이아웃 적용
  - 메시지/버튼 카피 정리
  - 오류/재시도 영역 위치 정리

## 🎯 MVP 범위 / 우선순위
- [x] MVP 필수 범위만 포함
- [x] Nice-to-have는 제외 또는 후속 이슈로 분리

이번 PR 우선순위:
- P0(필수): 로그인 진입 UX 단순화
- P1(중요): 후속 브랜딩 디테일/애니메이션
- P2(후순위): 전역 스타일 통일

## 🧪 테스트
- [x] 로컬 실행 확인 (`npm run dev` 등)
- [x] 핵심 시나리오 수동 테스트
- [x] 회귀 영향 확인

테스트 시나리오:
- `/auth?next=/today` 진입 후 카카오 버튼 노출/동작
- 로그인 실패 시 오류 메시지 + 다시 시도하기 노출

결과:
- [x] 통과
- [ ] 미통과(아래에 상세)

추가 검증:
- `apps/web npm test` 통과
- `apps/web npm run lint` 에러 0 (기존 warning 4)

## 📷 증빙
- 로그인 UI 관련 기존 기준 이미지: `docs/screenshots/2026-03-19-auth-social-p0.jpg`

## ⚠️ 영향도 / 리스크
- 영향 범위: web(auth UI)
- 리스크: 기능 로직은 유지했지만 UI 수정으로 모바일 WebView 체감 확인 필요
- 롤백 방법: 본 PR revert

## 🗂️ 관련 이슈
- Related #

## 🔁 배포/마이그레이션 체크
- [x] 환경변수 변경 없음
- [ ] 환경변수 변경 있음 (문서/시크릿 반영 완료)
- [x] DB 마이그레이션 없음
- [ ] DB 마이그레이션 있음 (적용/롤백 절차 기재)

## 📝 리뷰어 가이드
1. auth 첫 화면에서 카카오 단일 진입 의도 전달이 충분한지
2. 에러/재시도 영역 가독성
3. 기존 로그인 복귀 로직 회귀 여부
