## 📌 PR 요약
- 친구 연동 P0 진행을 위해 로그인 직후 프로필 자동 생성과 친구 요청 화면 로직을 정리했습니다.
- 핵심 변경:
  - 첫 로그인 시 `profiles` 자동 생성(닉네임/friend_code)
  - 친구 요청 목록을 받은/보낸/친구(accepted)로 분리
  - 수락 버튼을 받은 pending 요청에만 노출
  - `/auth?next=/friends` 리다이렉트 스크린샷 추가

## 🔎 문제 재현 (Before)
- [x] 재현 절차를 적었다
- [x] 기존 동작/에러를 캡처했다 (로그, 스크린샷, 영상 중 1개)

재현 절차:
1. 로그인 성공 후에도 profiles 레코드가 없으면 friend_code 기반 기능 실패
2. 친구 요청 목록이 받은/보낸 구분 없이 노출
3. pending이면 모두 수락 버튼이 보여 액션 혼선

기존 결과:
- friend_code/요청 상태 기반 UX가 불안정

## 🧠 원인
- 로그인 이후 profile bootstrap 로직 부재
- friendships 목록을 역할(요청자/수신자) 기준으로 분리하지 않음
- 관련 코드/모듈/데이터 흐름:
  - `apps/web/src/lib/profile-bootstrap.ts`
  - `apps/web/src/lib/friends.ts`
  - `apps/web/src/app/friends/page.tsx`

## ✅ 해결 (After)
- 로그인 세션 확인 시 `ensureMyProfile()`을 호출해 profile/friend_code를 보장
- friendships를 받은/보낸/accepted로 분리해 표시
- 수락 액션은 받은 pending에만 제공

변경 사항:
- `apps/web/src/lib/profile-bootstrap.ts` 추가
- `apps/web/src/lib/friends.ts` 개선 (split helper 추가)
- `apps/web/src/lib/friends.test.ts` 추가
- `apps/web/src/app/friends/page.tsx` UI/로직 개선
- `apps/web/src/app/auth/page.tsx` 로그인 후 profile bootstrap 연동
- `docs/friend-auth-p0-design.md` 업데이트

## 🎯 MVP 범위 / 우선순위
- [x] MVP 필수 범위만 포함
- [x] Nice-to-have는 제외 또는 후속 이슈로 분리

이번 PR 우선순위:
- P0(필수): profile bootstrap + 친구 요청/수락 흐름 안정화
- P1(중요): 친구 닉네임/프로필 표시 고도화
- P2(후순위): 차단/신고 UX

## 🧪 테스트
- [x] 로컬 실행 확인 (`npm run dev` 등)
- [x] 핵심 시나리오 수동 테스트
- [x] 회귀 영향 확인

테스트 시나리오:
- friends split 유틸 테스트
- 기존 auth/social/friend-code/auth-redirect 테스트 회귀
- 전체 단위 테스트 통과(24 tests)

결과:
- [x] 통과
- [ ] 미통과(아래에 상세)

## 📷 증빙
- 비로그인 상태 `/friends` 진입 시 로그인으로 리다이렉트 (`next=/friends`) 확인
  - ![auth-next-friends-redirect](https://raw.githubusercontent.com/jxxh204/routine-app/feat/profile-bootstrap-friends-status/docs/screenshots/2026-03-20-auth-next-friends-redirect.jpg)

## ⚠️ 영향도 / 리스크
- 영향 범위: web
- 리스크: profiles insert 권한/중복 정책과 실제 운영 데이터 상태 점검 필요
- 롤백 방법: 해당 커밋 revert

## 🗂️ 관련 이슈
- Closes #
- Related #

## 🔁 배포/마이그레이션 체크
- [x] 환경변수 변경 없음
- [ ] 환경변수 변경 있음 (문서/시크릿 반영 완료)
- [x] DB 마이그레이션 없음
- [ ] DB 마이그레이션 있음 (적용/롤백 절차 기재)

## 📝 리뷰어 가이드
- 중점 리뷰 포인트 1~3개
1. profile bootstrap 실패 시 UX/오류 처리 흐름 적절한지
2. 친구 요청 분리(incoming/outgoing/accepted)가 의도와 일치하는지
3. 수락 버튼 노출 조건이 받은 pending으로 제한되는지
