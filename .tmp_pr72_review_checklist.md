리뷰어 가이드 체크리스트 남깁니다.

### 1) /auth callback + next 복귀 안정성
- [ ] 로그인 성공 후 `/auth?next=...` -> `ensureMyProfile()` -> `next` 경로로 정상 이동하는지
- [ ] `next`가 비정상 값일 때(`/`, `//`, 외부 URL) 안전하게 `/today`로 폴백되는지
- [ ] sessionStorage 보관값이 성공 후 정리되는지

### 2) session-recovery 부작용 점검
- [ ] callback 직후 지연 상황에서 재시도로 세션이 복구되는지
- [ ] 무한 대기/무한 루프 없이 실패 시 사용자에게 메시지가 노출되는지
- [ ] AuthRequired 경로(`/today`, `/friends`)에서 회귀 이슈 없는지

### 3) 모바일 auth 화면 분리 + WebView OAuth 유지
- [ ] `/auth` 경로에서 상단 헤더/하단 탭이 숨겨지는지
- [ ] 카카오 OAuth 이동이 외부 브라우저가 아니라 WebView 내부에서 유지되는지
- [ ] 로그인 완료 후 보호 페이지 접근이 바로 가능한지

### 스모크 테스트(권장)
- [ ] 비로그인 `/friends` -> `/auth?next=/friends` -> 로그인 -> `/friends` 복귀
- [ ] 로그인 취소/실패 시 분리 에러 메시지 + `다시 시도하기` 동작
- [ ] 웹 테스트/린트/빌드 결과 확인 (PR 본문 참조)
