## 📌 PR 요약
- 비로그인 사용자가 `/today`만 열어도 로그인 페이지로 이동하도록 인증 가드를 추가했습니다.
- 핵심 변경:
  - 보호 라우트 인증 가드 컴포넌트 추가
  - `/today`, `/calendar`, `/settings`, `/friends`에 가드 적용
  - `/auth`에 `next` 파라미터 처리(로그인 후 복귀 경로)
  - 설계서 업데이트 + 리다이렉트 스크린샷 추가

## 🔎 문제 재현 (Before)
- [x] 재현 절차를 적었다
- [x] 기존 동작/에러를 캡처했다 (로그, 스크린샷, 영상 중 1개)

재현 절차:
1. 앱(WebView)이 `/today` 진입
2. 비로그인 상태여도 `/auth`로 이동하지 않음
3. 로그인 페이지가 안 보인다고 인지됨

기존 결과:
- 로그인 화면이 존재해도 기본 진입 라우트에서 노출되지 않음

## 🧠 원인
- 보호 라우트 인증 가드가 없어서 비로그인 상태를 라우팅에서 차단하지 못함
- 관련 코드/모듈/데이터 흐름:
  - `apps/web/src/components/auth-required.tsx`
  - `apps/web/src/lib/auth-redirect.ts`

## ✅ 해결 (After)
- 보호 라우트에서 세션이 없으면 `/auth?next=...`로 강제 이동
- 로그인 후 `next` 경로로 복귀 가능

변경 사항:
- `apps/web/src/components/auth-required.tsx` 추가
- `apps/web/src/lib/auth-redirect.ts` + 테스트 추가
- today/calendar/settings/friends 페이지 가드 적용
- `apps/web/src/app/auth/page.tsx` next 파라미터 처리
- `docs/friend-auth-p0-design.md` 업데이트

## 🎯 MVP 범위 / 우선순위
- [x] MVP 필수 범위만 포함
- [x] Nice-to-have는 제외 또는 후속 이슈로 분리

이번 PR 우선순위:
- P0(필수): 비로그인 진입시 로그인 페이지 유도
- P1(중요): 세션 만료/갱신 UX 고도화
- P2(후순위): 미들웨어 기반 서버단 가드

## 🧪 테스트
- [x] 로컬 실행 확인 (`npm run dev` 등)
- [x] 핵심 시나리오 수동 테스트
- [x] 회귀 영향 확인

테스트 시나리오:
- auth-redirect 유틸 테스트(경로/보안 검증)
- 비로그인으로 `/today` 진입 시 `/auth` 화면 노출 확인
- 기존 unit tests 회귀 확인

결과:
- [x] 통과
- [ ] 미통과(아래에 상세)

## 📷 증빙
- `/today` 비로그인 진입 후 `/auth` 리다이렉트 화면:
  - ![auth-guard-redirect](https://raw.githubusercontent.com/jxxh204/routine-app/feat/auth-guard-p0/docs/screenshots/2026-03-19-auth-guard-redirect.jpg)

## ⚠️ 영향도 / 리스크
- 영향 범위: web
- 리스크: Supabase env 미설정 시 항상 auth로 이동 (의도된 보호 동작)
- 롤백 방법: 해당 커밋 revert

## 🗂️ 관련 이슈
- Closes #
- Related #

## 🔁 배포/마이그레이션 체크
- [x] 환경변수 변경 없음
- [ ] 환경변수 변경 있음 (문서/시크릿 반영 완료)
- [x] DB 마이그레이션 없음
- [ ] DB 마이그레이션 있음 (적용/롤백 절차 기재)

## 📝 리뷰어 가이드
- 중점 리뷰 포인트 1~3개
1. 보호 라우트에서 비로그인 리다이렉트가 안정적으로 동작하는지
2. `next` 파라미터 처리에 오픈리다이렉트 취약점이 없는지
3. today/calendar/settings/friends 모두 동일 정책이 적용됐는지
