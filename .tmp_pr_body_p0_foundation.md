## 📌 PR 요약
- 친구 연동 P0 구현 착수를 위해 로그인 UI(카카오/애플)와 DB 권한(RLS) 기반을 추가했습니다.
- 핵심 변경:
  - `/auth` 소셜 로그인 화면 추가 (P0 provider 노출)
  - 소셜 OAuth 시작 유틸 + 테스트 추가
  - friend/auth P0 스키마에 RLS 정책 초안 추가
  - 설계서/DB README 업데이트 + 화면 스크린샷 첨부

## 🔎 문제 재현 (Before)
- [x] 재현 절차를 적었다
- [x] 기존 동작/에러를 캡처했다 (로그, 스크린샷, 영상 중 1개)

재현 절차:
1. 친구 연동 기능 시작 전 로그인 진입점(`/auth`) 부재
2. 소셜 provider 호출 유틸 미구현
3. friend/auth P0 스키마에 RLS 정책 미정의

기존 결과:
- 인증/친구 기능의 UI 진입과 권한 기반이 없어 다음 단계 구현이 막힘

## 🧠 원인
- P0 설계는 머지됐지만 실행 가능한 로그인 UI/유틸/RLS 초안이 아직 코드화되지 않음
- 관련 코드/모듈/데이터 흐름:
  - `apps/web/src/app/auth/page.tsx`
  - `apps/web/src/lib/social-login.ts`
  - `db/supabase/schema_v4_friend_auth_p0.sql`

## ✅ 해결 (After)
- 로그인 UI와 OAuth 시작 유틸을 먼저 고정해 프론트 착수 가능 상태로 전환
- DB RLS 초안을 추가해 friend/auth 데이터 접근 기준 명시

변경 사항:
- `apps/web/src/app/auth/page.tsx` 추가
- `apps/web/src/lib/social-login.ts` 추가
- `apps/web/src/lib/social-login.test.ts` 추가
- `db/supabase/schema_v4_friend_auth_p0.sql` RLS 정책 추가
- `docs/friend-auth-p0-design.md` 업데이트
- `db/supabase/README.md` 적용 순서 업데이트

## 🎯 MVP 범위 / 우선순위
- [x] MVP 필수 범위만 포함
- [x] Nice-to-have는 제외 또는 후속 이슈로 분리

이번 PR 우선순위:
- P0(필수): 소셜 로그인 진입/호출 기반 + RLS 초안
- P1(중요): 친구 요청/수락 API 및 UI
- P2(후순위): 독려 푸시 고도화

## 🧪 테스트
- [x] 로컬 실행 확인 (`npm run dev` 등)
- [x] 핵심 시나리오 수동 테스트
- [x] 회귀 영향 확인

테스트 시나리오:
- social-auth-policy: p0/p1 provider 반환 검증
- social-login: provider 매핑 및 오류 반환 검증
- 전체 단위 테스트 통과(14 tests)

결과:
- [x] 통과
- [ ] 미통과(아래에 상세)

## 📷 증빙
- 로그인 UI 스크린샷:
  - ![auth-social-p0](https://raw.githubusercontent.com/jxxh204/routine-app/feat/p0-auth-friends-foundation/docs/screenshots/2026-03-19-auth-social-p0.jpg)

## ⚠️ 영향도 / 리스크
- 영향 범위: web, db
- 리스크: RLS 정책은 앱 플로우와 맞춰 후속 PR에서 미세 조정 필요
- 롤백 방법: 해당 커밋 revert

## 🗂️ 관련 이슈
- Closes #
- Related #

## 🔁 배포/마이그레이션 체크
- [x] 환경변수 변경 없음
- [ ] 환경변수 변경 있음 (문서/시크릿 반영 완료)
- [ ] DB 마이그레이션 없음
- [x] DB 마이그레이션 있음 (적용/롤백 절차 기재)

## 📝 리뷰어 가이드
- 중점 리뷰 포인트 1~3개
1. /auth에서 P0 provider(카카오/애플)만 노출되는지
2. social-login provider 매핑/오류 처리 흐름 적절한지
3. schema_v4 RLS 정책이 P0 권한 모델과 일치하는지
